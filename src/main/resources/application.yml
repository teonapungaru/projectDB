spring:

  jpa:
    #    properties:
    #      javax:
    #        persistence:
    #          schema-generation:
    #            create-source: metadata
    #            scripts:
    #             action: create
    #             create-target: create.sql
    hibernate:
      ddl-auto: create-drop
      dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

  datasource:
    url: jdbc:mysql://aa1r5ki3mw3v4q9.crqr2khb6ff5.eu-central-1.rds.amazonaws.com/chronos_dev4?useSSL=false
    username: chronos_dev4
    password: chronos_dev4
    initialization-mode: always
    platform: mysql
  jackson:
    serialization:
      write-dates-as-timestamps: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG